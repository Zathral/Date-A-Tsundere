# Tsundere Dashboard Exact Reference Layout V8

Fixed the icon crops by replacing the brittle screenshot crops with clean transparent reference-style icon assets, then tightened the left status layout so labels, numbers, and bars no longer collide.

Open `index.html` in a browser.
